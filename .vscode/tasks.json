{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "gprbuild",
            "args": [
                "-p",
                "-P",
                "${workspaceFolder}/gnat/lsp_server.gpr",
                "-cargs",
                "-gnatef"
            ],
            "problemMatcher": ["$ada"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}